// interfaces/index.ts

// Props for the Card component
export interface CardProps {
  // Add properties like title, content, etc.
}

// Props for the Button component
export interface ButtonProps {
  // Add properties like label, onClick, etc.
}
